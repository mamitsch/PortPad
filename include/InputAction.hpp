#pragma once

namespace portpad {
// Device-independent actions shared by keyboard, controller, and application logic.
enum class InputAction { Up, Down, Left, Right, Confirm, Back, Menu, PageUp, PageDown, Details };
}
